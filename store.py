import pandas as pd

store = df.Dataframe{'name':['Cursor','Grandma'],
        'css_tag_price':['productPrice0','productPrice1'],
        'cookie_per_seconds':[0.1,1],
        'css_tag_button':['Product0','Product1'],
        'unlock_in':[0,100]}

def next_buy(df,count_total_cookies):
    df = df[cookies >= df['unlock_in']]
    for index, row in df.iterrows():
        css_tag_price = row['css_tag_price']
        price = browser.find_element_by_id(css_tag_price)
        price = int(button.text)
    

